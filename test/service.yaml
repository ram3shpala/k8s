apiVersion: v1
kind: Service
metadata:
  name: quiz-service
  namespace: demoapps
spec:
  selector:
    app: quiz-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32042
  type: NodePort
